phase:         distill
data_dir:      data/raw
student_lr:    0.001
weight_decay:  0.0001
label_smoothing: 0.1
epochs:        20
batch_size:    32
num_workers:   4
val_split:     0.15
test_split:    0.10
lr_patience:   3
early_stop_patience: 7
checkpoint_dir: checkpoints
seed:          42
teacher_checkpoint: checkpoints/phase_b_best.pt
temperature:   3.0
alpha:         0.5
